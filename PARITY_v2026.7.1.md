# MyLobster ↔ OpenClaw v2026.7.1 Parity Tracker

**Status:** mylobster carries the v2026.4.29 backlog (`PARITY_v2026.4.29.md`) and the v2026.5.2 backlog (`PARITY_v2026.5.2.md`) as **parent backlogs** — both remain valid on top of this file. This file tracks only the NEW delta that landed upstream after v2026.5.2, up to and including v2026.7.1 (~25,181 commits across v2026.5.3 → v2026.7.1).

**Source:** `agent/openclaw/CHANGELOG.md` lines 5–7341 (v2026.7.1 down to, not including, v2026.5.2). Tag pinned at `v2026.7.1` (commit `2d2ddc43d0d`). Previous baseline `v2026.5.2` (commit `d026b7b233c`).

**Scope rule:** items below have a plausible Rust analog in mylobster. Pure npm distribution / Docker / dist packaging / pnpm workspace / TypeBox / generated config schema / `dist/` chunks / Bun plumbing / Node-version-floor items are out of scope and not listed. The parity target is OpenClaw TS at the named tag — items are never marked out-of-scope merely because mylobster currently lacks the subsystem; those get ❌ with a "new file" landing site.

**Version tags:** rows spanning multiple releases carry a coarse range, e.g. *(5.x)* = v2026.5.3–5.31, *(6.x)* = v2026.6.1–6.11, *(7.1)* = v2026.7.1.

**Conventions:**
- ✅ Done in mylobster
- 🚧 Partial
- ❌ Not yet
- *(file)* — natural landing site

---

## Providers (`src/providers/`)

| Item | Status | Notes |
|------|--------|-------|
| Model catalog refresh: Claude Sonnet 5, Mythos 5, Opus 4.8, Haiku 4.5, Fable 5; GPT-5.6 series (new-setup default, `/think ultra`/`max`), GPT-5.5, GPT-5.3 Codex Spark; GLM-5/5.2; Kimi K2.5/K2.6/`kimi-k2.7-code`; Gemini 3.5 Flash (1M) + `gemini-3.1-flash-lite`; MiniMax M3; `mistral-medium-3-5`; NVIDIA Nemotron 3 Ultra / Super 1M; Xiaomi `mimo-v2.5[-pro]`; DeepSeek V4 tiers; prune retired Grok 3/4-Fast, old-Claude, Groq rows w/ doctor migration *(5.x–7.1)* | ❌ | `providers/catalog.rs` |
| New providers: Featherless; Meta `muse-spark-1.1` (Responses API, encrypted reasoning replay, live model validation); ClawRouter (credential-scoped dynamic discovery, budget/quota reporting); Tencent Hy3; LongCat; Cohere plugin; `ds4` local DeepSeek V4 Flash with `localService` on-demand startup *(6.x–7.1)* | ❌ | new modules under `providers/` |
| Gemini model-id migration: retired `gemini-3-pro-preview` → `gemini-3.1-pro-preview` across config load/write, onboarding, catalog, auth-patch, nested proxy forms (`kilocode/…`, `openrouter/google/…`) *(5.12–5.14)* | 🚧 | `providers/gemini.rs::normalize_google_model_id` / `normalize_google_model_ref` (all upstream id mappings incl. nested `kilocode/…`/`openrouter/google/…` forms; applied at `GeminiProvider::new`); config-load/write + catalog rewrite wiring still ❌ (`config/io.rs` / `catalog.rs` are other clusters) |
| `openai-codex` folded into `openai`: doctor repairs `openai-codex/*`→`openai/*`, Codex runtime policy scoped per provider/model, `oauthRef`/`auth.order.openai` OAuth profiles, `openai/chat-latest` override *(5.x–6.x)* | ❌ | `openai.rs`, `openai_codex.rs`, doctor |
| Codex app-server harness: managed `@openai/codex` binary (≥0.143 wire, 0.144.x), native threads shared across clients, per-agent `CODEX_HOME`, SQLite thread bindings, idle/turn-completion/post-tool watchdogs, `on-failure`→`on-request`/`ask` approval migration, native-thread overflow rotation, sandbox gating (`sandbox_exec`), OpenClaw `before_tool_call` policy authoritative over native shell, JSON-RPC error codes, `mcp.servers.<name>.codex.agents` scoping *(5.x–7.1)* | ❌ | new `agents/codex_runtime.rs` + `providers/openai_codex.rs` |
| Anthropic: 1M-context GA migration (drop `context-1m-2025-08-07` beta), interleaved text/thinking/tool deltas attached to correct block, thinking-block strip when disabled, stale signature strip + seeded-signature preservation, defer stream start until `message_start`, consecutive assistant-turn merge, tuple-schema normalization (Opus 4.8), Fable 5 safety-decline → Opus 4.8 fallback, `cache_control` re-mark stop on Vertex *(5.x–7.1)* | ❌ | `anthropic.rs` |
| OpenAI: SSE parse for streams mislabeled as JSON, refusal → assistant text, storeless replay (no item-id reuse), invalid reasoning-signature recovery, cumulative-final dedupe (incl. Bedrock Mantle GPT-5.x), stop sequences, `tool_choice` required/function contracts, `reasoning_tokens` without double-count, clamp `input_tokens-cached_tokens`≥0, per-model `max_completion_tokens`, parallel tool-call arg buffers separated, deterministic tool-payload ordering for prompt cache *(6.x–7.1)* | ❌ | `openai.rs` |
| Azure OpenAI Responses: default unset API version to `preview` (routes `/openai/v1/responses`), bounded first-event diagnostics on stream stalls *(5.14)* | ❌ | `openai_compat.rs` |
| OpenAI-compat capability knobs: `compat.thinkingFormat` (`qwen`/`qwen-chat-template`/`together`), `compat.strictMessageKeys`, `compat.supportsTools:false` strips tool payloads, model-declared unsupported-schema-keyword stripping, SSE/JSON fallback drain across split chunks, `stream_options.include_usage` opt-in (Volcengine/Ark) *(5.x–6.x)* | ❌ | `openai_compat.rs` |
| OpenRouter: strip duplicated `openrouter/` prefix, explicit auth headers, opt-in cache headers (`X-OpenRouter-Cache*`), 403 budget-limit → billing classification, `supported_parameters` tool metadata, non-replayable provenance-tag strip, streamed generation-cost reconciliation, bounded oversized catalogs *(5.x–7.1)* | ❌ | `openrouter.rs` (new file — mylobster routes OpenRouter via compat today) |
| Reasoning-content replay hardening across schema-strict providers: Kimi (cache-marker strip, `reasoning_content` + tool placeholders), DeepSeek (`anyOf` normalize, tier-suffix replay, empty-placeholder strip, DSML leak suppression, `xhigh`/`max` levels), Xiaomi/MiMo (replay as thinking blocks), MS Foundry (Responses API + thinking-param skip on fallback), SGLang history preserve, one shared `reasoning_effort`/thinking-level normalization helper *(5.x–6.x)* | ❌ | `openai_compat.rs` + provider shims |
| Kimi/Moonshot: alias canonicalization (`moonshotai/`→`moonshot/`, `kimi-code`→`kimi-for-coding`), thinking budgets/output room, `minLength` constraint strip, 429-billing permanent classification *(5.x)* | ❌ | provider catalog + compat |
| Ollama: `params.num_ctx` doctor copy + no silent catalog auto-copy, `*:cloud` idle watchdog, discovery preserves configured API + live catalog metadata, unknown-capability models default tool-capable, `top_p` normalization, Docker/OrbStack aliases as local, native tool-call ID preservation, auto-discover inference nodes, incomplete-stream fallback classification *(5.x–7.1)* | ❌ | `ollama.rs` |
| LM Studio: quantized multi-variant models (no phantom suffixed entries), binary thinking on/off → `reasoning_effort`, Responses tool-arg preservation, embedding-preload context limits, repaired tool-call terminal event before completion *(6.x–7.1)* | ❌ | new `providers/lm_studio.rs` *(carryover from v5.2 — still absent)* |
| Google/Vertex auth: ADC (Workload Identity Federation, service-account, metadata-server), eu/us multi-region → `.rep.googleapis.com`, provider-prefix strip from Vertex model path, compressed token refresh, per-LLM-request API key rotation, Gemini CLI OAuth via env proxy + isolated runtime home *(5.x–7.1)* | 🚧 | `providers/gemini.rs`: eu/us → `.rep.googleapis.com` (`resolve_vertex_base_host`), `strip_google_provider_prefix`, per-request key rotation (`with_extra_api_keys` + 429 rotation, `collect_gemini_api_keys`) ✅; ADC/WIF/metadata-server token flows, compressed token refresh, CLI OAuth env-proxy/isolated-home still ❌ (needs Vertex OAuth infra) |
| Gemini behavior: preserve/recover `thought_signature` on replay (drop compaction-truncated), native reasoning mode, `reasoning: false` honored, forward stop sequences, ordered parallel tool responses, empty grounding metadata tolerated, `freshness: "day"` search fix, Live function-declaration normalization *(5.x–7.1)* | 🚧 | `providers/gemini.rs`: signature helpers (`is_valid_thought_signature` base64 drop, `retain_thought_signature`, `resolve_thought_signature`), native `thinkingConfig` (incl. `reasoning:false` per-family disabled configs), ordered parallel functionCall parsing, function-declaration schema sanitization, tools/toolConfig wiring ✅; grounding-metadata tolerance + `freshness:"day"` fix ✅ (`web_search/gemini.rs`); stop-sequence forwarding ❌ (needs `ProviderRequest.stop_sequences` — shared-type change, handoff to providers cluster) |
| xAI/Grok: device-code + SuperGrok OAuth (PKCE, loopback callback), OAuth reuse for `web_search`, `/think` support, reasoning-effort only for Grok 4.3, `grok-imagine-image-quality`, pending video-job polling, Composer reasoning-context preservation *(5.x)* | ❌ | `xai.rs` |
| Bedrock: `serviceTier` param (`default`/`flex`/`priority`/`reserved`), shared AWS profile cred refresh without restart, inference-profile prefix strip for embeddings, bounded Mantle catalog fetch, SDK auth for image/PDF tools, Bedrock/Mantle externalized packaging *(5.x–7.1)* | ❌ | `bedrock.rs` |
| Copilot: catalog refresh from `/models`, token exchange with `vscode-chat` identity for image, BYOK bearer through proxy, undefined `runtime.state` guard, Claude-sessions-after-empty-tool-error fix, Opus 4.8/1M caps *(5.x–7.1)* | ❌ | `copilot.rs` |
| Z.AI/GLM: stop GLM-5 fallthrough to OpenAI (misleading key errors), Zhipu overload classification → correct failover, graded thinking (`high`/`max` → reasoning effort) *(6.x)* | ❌ | `providers/mod.rs` routing + compat |
| Provider auth-state prewarm at startup (~20s → 5ms), deferred past readiness, coalesced/cancellable rewarms *(6.x)* | ❌ | `providers/mod.rs` |
| SecretRef egress-time injection: resolved secrets stay behind process-local sentinels through auth storage/stream setup/SDK config; plaintext injected only at final network boundary *(7.1)* | ❌ | `infra/secrets/` + provider transports |
| Bounded body reads + provider-owned malformed-response rejection across all provider/media/catalog/polling fetches (Moonshot, MiniMax, Inworld, Voyage, Copilot-token, Google-OAuth, Runway, FAL, DashScope, ElevenLabs, Deepgram, …) *(6.1–7.1)* | ❌ | shared HTTP helper in `providers/mod.rs` |
| Error classification: rate-limit before timeout in `detectErrorKind`, detail-less failures don't cool API-key profiles, retryable overload wording, transport-timeout cooldown scoped to model *(6.x–7.1)* | ❌ | `agents/model_fallback.rs` |
| Media-gen providers: fal Krea / GPT Image 2 / Nano Banana 2 `/edit` routing + input caps, `music_generate` (fal MiniMax/ACE/Stable Audio, OpenRouter Lyria), Pixverse video plugin, LiteLLM image *(5.x–6.x)* | ❌ | `media/mod.rs` |
| Core OpenAI-compatible embedding provider (config + doctor + docs) *(6.2)* | ❌ | `memory/embeddings.rs` |
| Provider manifest hygiene: `stripPrefixes` tolerant of spaces/casing, self-prefix strip, per-model `api`/`baseUrl` overrides, capability overlays clear inherited `providerOptions`, isolate invalid plugin model catalogs *(6.x)* | ❌ | `catalog.rs` |
| Model resolution fail-closed: unknown model auth, provider-qualified alias collision avoidance on session switches, `models.mode='replace'` skips implicit discovery, no synthesized `maxTokens` fallback, suppress retired refs, bound `/models` catalog loading *(6.x–7.1)* | ❌ | `providers/mod.rs` |
| Misc providers: Vercel AI Gateway live-only model IDs; Cloudflare AI Gateway preserves `x-api-key`; Together `reasoning.enabled`; Arcee Trinity Large tool-incompatible flag; Fireworks/BytePlus Kimi thinking-off; Qwen/vLLM chat-template preservation on thinking toggle *(5.x–6.x)* | ❌ | respective provider shims |
| `openclaw promos` — discover/claim promotional model offers, auto-detected plans/billing *(7.1)* | ❌ | `cli/mod.rs` + ClawHub client |

---

## Channels (`src/channels/`)

| Item | Status | Notes |
|------|--------|-------|
| Telegram durable ingress: isolated ingress worker + durable local spool (survives event-loop stalls), turn adoption (complete spooled updates only after adoption), dead-letter/tombstone poison updates, offset persistence after dispatch, `getUpdates` conflict/pid-reuse recovery, polling stall thresholds (`pollingStallThresholdMs`), watchdog keyed to `getUpdates` liveness, restart-dropped inbound media retry *(5.x–7.1)* | ✅ | `telegram_spool.rs` (SQLite spool: dedupe by update_id, adoption-completes, 3-attempt dead-letter tombstones, offset persisted after dispatch keyed by token fingerprint) + `telegram_dispatcher.rs` (poll→spool→lane dispatch loop, 409 conflict backoff+recovery, watchdog on `TelegramPollingLivenessTracker` with clamped `pollingStallThresholdMs`). Restart-dropped media retry: spool replay covers it (offset stored post-dispatch, re-poll dedupes) |
| Telegram rich HTML pipeline: single rich-HTML normalizer with rich→plain fallback, table normalization before entity-escaping, expandable blockquotes, code-fence language preservation, markdown link hrefs, sticker paths, `richMessages` flag (default false), UTF-16 surrogate guards *(6.x–7.1)* | ✅ | `telegram_format.rs`: normalizer + rich→plain fallback, tables→`<pre><code>` pre-escape, expandable blockquotes, fence languages, link hrefs, UTF-16 guards; `render_rich_message_chunks` (32k/500-block/50-media caps) + `TelegramApi::send_rich_markdown` behind `richMessages` (default false); sticker paths via `parse_sticker_reference` → native sendSticker in `send_message_chunked` |
| Telegram streaming/progress: draft-preview rotation + dedupe across streaming modes, multi-lane reasoning/commentary progress in one stable window, clear old progress bubble before tool output, headings shown once, verbose status isolated after streamed finals *(5.x–7.1)* | ✅ | `telegram_progress.rs`: `ProgressComposer` (multi-lane reasoning/commentary/tool, stable order, headings-once) + `TelegramProgressWindow` (durable edits, dedupe on unchanged render, rotation past chunk limit, recreate-on-gone, `clear_before_tool_output` delete, `finalize` isolates post-final verbose status to a new message) |
| Telegram routing: forum-topic lanes + `:topic:` suffixes + topic-name propagation + fail-closed thread-not-found, per-topic/DM concurrent drain, reply-chain media hydration + persisted reply-context cache, mention → assistant identity binding, `/command@TargetBot` handling, foreign `/stop@otherbot` stays on topic lane, account-scoped topic → correct agent *(5.x–6.x)* | ✅ | `telegram_targets.rs` (full targets port) + `telegram_dispatcher.rs`: per-topic/DM lanes (`LaneKey`, sequential-in-lane, concurrent-across via join_all), reply-chain hydration from persisted `telegram_reply_context` (SQLite), mention→identity binding from cached getMe (`mention_regexes`), `/command@TargetBot` parsing with foreign `/stop@otherbot` staying on-lane unexecuted, account-scoped topic→agent via `resolve_topic_agent` (`agentId` on group/topic config); thread-not-found fails closed |
| Telegram transport: sticky IPv4 Bot API with re-probe, method-aware request timeouts, flood-wait recovery on final sends, ENETDOWN/HTTP 421 retry, `sendChatAction` cooldown, structured `error_code` 401 classification, token-rotation offset discard, `getMe` identity cache, outbound drain after reconnect, durable `sendMessage` retry *(5.x–7.1)* | ✅ | `TelegramApi`: live sticky chain primary→IPv4-only (local_address 0.0.0.0)→pinned-IP (resolve override) with debug promotions/recoveries + warn pinned, 20-success primary re-probe; method-aware timeouts; `call_with_send_retry` (flood-wait ×2 capped 30s, 421/pre-connect one-shot); sendChatAction cooldown guard; structured 401; fingerprint offset discard; `get_me_cached`; outbound drain after reconnect in `telegram_dispatcher.rs` (bounded queue drained on transport recovery) |
| Telegram features: `/steer` + `/tell` on active runs, Codex pairing via private `/login`, Mini App `web_app` buttons, native command localization + limits, 10-option poll cap, `channels.telegram.mediaGroupFlushMs`, `trustedLocalFileRoots` local Bot-API paths, DM exec-approval allowlist with `ask:off`, admin required for target writeback *(5.x–7.1)* | ✅ | `/steer`+`/tell` in dispatcher via `ActiveRunSink` (default `QueueNextTurnSink`→`agents::run_single_message`; documented seam — port has no streaming-run registry); `/login` in `telegram_pairing.rs` (owner-gated, DM-only, provider parse, per-chat flow reservation, 10-min expiring codes, `LoginFlowRunner` seam); `web_app` buttons; localization (`descriptionLocalizations`, per-locale variants registered with language_code in both scopes); 10-option poll cap; `mediaGroupFlushMs`; `trustedLocalFileRoots` canonicalized-prefix check; DM exec-approval allowlist honored with ask:off (`resolve_dm_exec_approval`) |
| WhatsApp plugin modernization: Baileys 7.x line, `StatusReactionController` lifecycle (queued→thinking→tool→done/error) at Telegram/Discord parity, LID forward-mapping + `@digits` mention metadata + LID JID via `authDir`, `@newsletter` channel targets, suffix-only streaming (no cumulative preambles), group message-tool-only visible-reply default *(5.x–7.1)* | ❌ | `whatsapp.rs` |
| WhatsApp durability: auth preserved on terminal disconnects + durable persist before login-success, malformed-creds restore from backup, reject symlinked `creds.json`, bounded reconnect catch-up, periodic outbound drain, stop restart loops after logout/replaced, expired terminal QR replacement + 408 retry, serialized overlapping Web sends *(6.x–7.1)* | ❌ | `whatsapp.rs` |
| WhatsApp context/media: group context across retries/reconnects, replies stay quoted to triggering message, bot-authored quote metadata, inbound admission + media size caps, `forceDocument`/`asDocument` original bytes, MIME-named documents, `group:`-prefixed JID resolution, reachout timelock, quoted-image reply media *(5.x–7.1)* | ❌ | `whatsapp.rs` |
| Discord voice overhaul: `/vc` STT/TTS/talk-buffer/bidi modes, `agent-proxy` default with forced consult, `voice.allowedChannels`, barge-in (`minBargeInAudioEndMs`, `captureSilenceGraceMs`), follow users into channels, multi-user handoff, DAVE recovery, per-account voice isolation, auto-join race close, ffmpeg stderr handling, opusscript default + opt-in `@discordjs/opus` *(5.x–7.1)* | ✅ | Bundled-native, upstream is plugin. `channels/discord_voice.rs`: `/vc` join/leave/status/mode parsing, consult-policy default `always` for agent-proxy, `allowedChannels` gate, barge-in policy (wake-name gating disables; 250ms min playback), follow-users, wake-name resolution/matching, per-account connection groups, DAVE/connect/reconnect/capture-grace knobs in `DiscordVoiceConfig`+`DiscordVoiceRealtimeConfig`. Live audio stack (DAVE crypto recovery, ffmpeg stderr, opus codecs, multi-user mixing) is policy-only — needs voice deps the port doesn't take |
| Discord streaming/status: progress-draft streaming default, `statusReactions.timing` + `trackToolCalls`, tool-status emojis before thinking reactions, finals as fresh messages (mark unread) without `@everyone` escalation, long replies with code fences near 2000-char limit, suppress tool progress for message-tool replies, sanitize tool progress *(5.x–7.1)* | ✅ | Bundled-native, upstream is plugin. `channels/discord_chunk.rs`: fence-balanced chunking (2000 chars/17 lines, fence-close reservation, CJK/whitespace breaks, reasoning-italics rebalance). `channels/discord_status.rs`: `StatusReactionEngine` (queued→thinking→tool→done/error, `messages.statusReactions` config + timing, tool-category emojis, stall ⏳/⚠️), `trackToolCalls` react-binding gate, front-channel sanitize, tool-progress suppression for message-tool replies, progress default stream mode, fresh-final allowed-mentions without `everyone` |
| Discord routing: thread bindings persisted in SQLite + routed to plugin owners, mention aliases protected in code fences, cross-provider guild admin action block, PluralKit DM pairing + dedupe, attachment download at receipt time, implicit-reply fanout limit, alpha-bucket model picker >25, component registry `agentComponents.ttlMs`, default `suppressEmbeds` *(5.x–7.1)* | ✅ | Bundled-native, upstream is plugin. `channels/discord_routing.rs`: `DiscordThreadBindingStore` (rusqlite, owner-routed, idle/max-age prune, `threadBindings` config), guild-admin action block enforced in `discord_actions.rs` (non-Discord session keys rejected), PK sender identity for pairing (`resolve_discord_sender_identity`), reply-reference fanout limit (`first`/`batched` single-use), `compute_alpha_buckets` (letter groups never split, 25-bucket cap), `ComponentRegistry` + `agentComponents.ttlMs` (30min default), `suppressEmbeds` default-true + message flags. Mention-aliases-in-code-fences shipped in the v5.2 batch (`rewrite_discord_known_mentions`). Attachment download at receipt: runtime media pipeline not wired in port — receipt-time semantics noted in code, no separate impl |
| Discord transport: IPv4-preferred REST/gateway, configurable READY timeouts, bounded gateway websocket/metadata payloads, failed-resume recovery, error 10065 as disconnected, drop-during-reconnect fix, slash-command deploy hash persistence, learned 429 bucket cooldowns *(5.x–7.1)* | ✅ | Bundled-native, upstream is plugin. `channels/discord_transport.rs`: IPv4-first DNS ordering for discord.com/gg hosts, 16MiB gateway + 1MiB metadata payload bounds + `gatewayInfoTimeoutMs`, `GatewayResumeState` (failed resume → full re-identify, 50-attempt budget), `ReconnectSendBuffer` (ordered drain, bounded, no drops mid-reconnect), 10065/"unknown voice state" → disconnected classification, `CommandDeployCache` (sha256 stable hash, app-scoped keys, JSON persistence). READY timeouts + learned 429 bucket cooldowns shipped in the v5.2 batch (`GatewayReadyWatch`, `RateLimitBook`) |
| Slack native assistant threads: App Home + assistant thread lifecycle (manifest, suggested prompts), native thread status + rotating loading messages gated by `messages.statusReactions.enabled`, thread-scoped session routing + participation store surviving restarts *(5.x–7.1)* | ❌ | `slack.rs` |
| Slack routing/config: router relay mode (central router → owning gateway), `unfurlLinks`/`unfurlMedia` per-account (default off), `replyBroadcast`, `allowBots:"mentions"`, `ignoreOtherMentions`, per-channel `replyToMode`, DM canonicalization to peer session, fail-closed mention detection, name-vs-ID channel map warnings *(5.x–7.1)* | ❌ | `slack.rs` |
| Slack robustness: SecretRef tokens/signing secrets, Socket Mode native reconnect (no cap), process-wide write serialization + serialized inbound lookups, unbounded thread pagination, Block Kit `streaming.progress.render:"rich"`, `view_submission`/`view_closed` routing, reasoning-payload suppression, attachment text in thread context, bot-token-as-user warning *(5.x–7.1)* | ❌ | `slack.rs` |
| Matrix: externalized plugin (SDK out of core), E2EE memory-leak fix, forced cross-signing reset w/ recovery key, sqlite-backed sync cache + crypto sidecars, MSC4222 `state_after` stop, `com.openclaw.presentation` rich-render metadata, markdown tables → bullet lists, bracketed display-name mentions, room-id case preservation, two-person-room routing over stale `m.direct`, approval-reaction persistence across restarts *(5.x–7.1)* | ❌ | `matrix.rs` |
| iMessage: BlueBubbles channel removed → `channels.imessage` migration (`imsg` backend), `imsg rpc` / `send-rich --file` (reactions/edits/replies/attachments), `catchup.*` offline replay with persisted cursor, inbound tapbacks (`reactionNotifications`), native polls (create/read/vote, echo suppression), per-group `systemPrompt` + wildcard, HEIC→JPEG staging, monitor state → SQLite, durable echo markers, always-on inbound recovery + dedupe, self-chat timestamp-skew tolerance *(5.x–7.1)* | ❌ | `imessage.rs` (retire `bluebubbles.rs`) |
| Feishu streaming cards: merged text each streaming update, CardKit delivered-deltas + retry, Schema 2 card callbacks (`operator.user_id`), `channels.feishu.blockStreaming` + blocks as independent messages, card-JSON sent as cards, 30s streaming-card timeout, fallback when accepted turn has no visible reply *(5.x–7.1)* | ❌ | `feishu.rs` |
| Feishu ops: native topic threading + `replyInThread`, native `at`/post mentions + fail-closed mention forwarding without bot Open ID, rate-limit retries (429/230020/230006/11232), redelivery dedupe by retry identity, self-echo drop, bitable tool gating + non-empty write schema, wiki/drive pagination, CJK filename recovery, voice reply duration + Ogg muxer, per-chat sequential-queue eviction *(5.x–7.1)* | ❌ | `feishu.rs` |
| Signal: target aliases, native reply quotes, inbound status reactions, group allowlists match inbound group/sender ids + mixed-case ids, digit-free identity rejection, bounded receive payloads, `mediaMaxMb` cap, long-lived receive SSE *(5.x–7.1)* | ❌ | `signal.rs` |
| MS Teams: threaded reply routing for proactive sends, persisted sent-message markers (TTL), stores → SQLite, service-URL trust + attachment-fetch DNS validation, JWKS/Bot Connector egress diagnostics, admin-only group actions *(5.x–7.1)* | ❌ | `teams.rs` |
| Google Chat: new DMs route to 1:1 (not group), native approval card actions + click handling, thread metadata from sends, message-tool thread replies, auth-transport isolation + header normalization + webhook rate-limit, hidden internal failure banners *(5.x–6.x)* | ❌ | `googlechat.rs` |
| QQBot: `/bot-group-allways on|off` mention toggle (per-account + `defaultRequireMention`), framework command auth + command-turn marking, markdown table chunk validity, outbound reasoning-tag sanitization, state → SQLite KV, sandbox media send scoping, C2C typing window, watchdog from configured timeouts, failed-media surfacing *(5.x–7.1)* | ❌ | new `channels/qqbot.rs` *(carryover: file still absent)* |
| LINE: webhook ack before processing, trailing-slash path canonicalization, carousel column 60-char cap, push-target recovery *(5.x–6.x)* | ❌ | `line.rs` |
| IRC / Twitch / Nostr: IRC UTF-16 PRIVMSG chunking + 512-byte truncation + monitor reconnect + canonical channel routes; Twitch chat-intent auth/token refresh + account-lifetime keepalive; Nostr `npub` allowlist hex decode *(6.x–7.1)* | ❌ | `irc.rs`, `twitch.rs`, `nostr.rs` |
| Zalo: opaque string chat IDs, alternate Bot-API roots, native identity fields, hosted media in plugin state *(6.x–7.1)* | ❌ | `zalo.rs` |
| Twilio SMS channel (webhook diagnostics, proof follow-ups) *(6.1)* | ❌ | new `channels/sms.rs` |
| Nextcloud Talk / Synology / Mattermost misc: Talk file-share/lifecycle events tolerated + required `response` feature; Synology >120s replies; Mattermost message-tool replies stay in threads, `/oc_queue` slash, thread participation across restart (7-day idle), attachments via upload, channel-vs-group identification *(5.x–6.x)* | ❌ | `nextcloud.rs`, `synology_chat.rs`, `mattermost.rs` |
| Channel-turn kernel bot loop-guard: shared per-pair `channels.defaults.botLoopProtection` (`maxEventsPerWindow:20`, `windowSeconds:60`, `cooldownSeconds:60`) *(5.x)* | ❌ | `channels/mod.rs` kernel |
| Unified `streaming.mode:"progress"` drafts: shared progress-draft compositor (commentary/reasoning in drafts), auto labels, `streaming.progress.{label,labels,maxLines,toolProgress,commandText}`, failed-draft-start recovery *(5.x–6.x)* | ❌ | `channels/mod.rs` + per-channel senders |
| `channel-message` lifecycle adapter: `defineChannelMessageAdapter`, `prepareSendPayload`, durable message receipts rolled out across ~15 channels *(5.x)* | ❌ | `channels/mod.rs` trait + `plugins/api.rs` |
| Cross-channel media: caption preservation + explicit unavailable-attachment notices (no phantom placeholders), generic poll sender + typed presentation actions (approval/command/URL/web-app/select), `MEDIA` directives as attachments, home-relative `MEDIA:~/` paths *(6.x–7.1)* | ❌ | `channels/normalize.rs` + `infra/delivery.rs` |
| Cross-channel session identity: shared DM session must not carry previous channel identity after switch (status/reactions/threads target current), per-channel-peer session isolation, merged delivery routes account-bound, `messages.responsePrefix`, per-channel queue overrides (`messages.queue.byChannel.*`) *(6.x–7.1)* | ❌ | `routing/mod.rs` + `sessions/` |
| Control commands: bare `stop`/`abort`/`wait` as immediate control, `/verbose on` across chat types, fast-abort `/stop`, raw provider errors suppressed from chat, strip internal tool-trace banners + web-search citation markers from outbound *(6.x–7.1)* | ❌ | `gateway/chat.rs` + reply sanitize |
| Channel config hygiene: `enabled:true`-only entries treated as configured, malformed account specs rejected (`matrix:work:extra`), `channels status --json` keeps configured channels visible, scheduled announcements reject stale/ownerless destinations, channel capability checks return clear timeout *(6.x)* | ❌ | `channels/mod.rs` + `config/validation.rs` |

---

## Gateway / RPC (`src/gateway/`)

| Item | Status | Notes |
|------|--------|-------|
| New RPCs: `tts.speak` (operator-scoped inline audio), `system.info` (host/OS/runtime/CPU/mem/disk), terminal detach/reattach + `terminal.list`/`terminal.text`, read-only `agents.workspace` list/get, session-organization RPCs (groups, unread, rename, fork, archive, delete) *(7.1)* | ❌ | `gateway/websocket.rs` + new handlers |
| Task ledger `tasks.list`/`tasks.get`/`tasks.cancel` for async media/agent tasks *(5.12–5.14)* | 🚧 | RPC names exist in `gateway/websocket.rs` (v2026.3.11 work); verify ledger semantics + shared async task lifecycle |
| `talk.session.*` RPC family + unified Talk session controller; `cron.get` RPC *(5.x–6.x)* | ❌ | `gateway/websocket.rs` (`talk.config/mode/wake` present; session controller RPCs absent) |
| Descriptor-backed gateway method registry: plugin-owned RPC methods with scope metadata, hidden core collision checks, advertised vs internal handlers, `plugins.sessionAction` dispatch *(5.14–6.x)* | ❌ | `gateway/mod.rs` dispatch |
| Protocol v4 stream frames: explicit chat `deltaText`/`replace` frames, compatibility-range advertisement, protocol-mismatch diagnostics *(5.12–6.1)* | 🚧 | `gateway/protocol.rs` (`PROTOCOL_VERSION=4` + `min_protocol` negotiation present; delta/replace frames + mismatch diagnostics ❌) |
| OpenAI-compatible HTTP surface: `/v1/chat/completions` function `tools`/`tool_choice`/`tool_calls`/`role:tool`, `response_format`, `max_completion_tokens` mapping, OpenAI-shaped 400s, `/v1/responses` file-only + image-only input, early assistant-role SSE chunk, malformed-Host-safe path parsing *(5.x–6.x)* | ❌ | `gateway/routes.rs` |
| Crash-loop safe mode: persist boot outcomes, control-plane-safe mode after repeated unclean starts (transports/providers held until recovery), exit `EX_CONFIG` on fatal config errors, sysexits 78 on lock/EADDRINUSE *(7.1 + 5.2-carryover)* | ❌ | `main.rs` + `gateway/server.rs` |
| Restart/handoff: durable state-DB restart handoff (discard stale/wrong-process/superseded), `restart --safe --skip-deferral`/`--force`/`--wait`, active-blocker counting, drain pending replies + abort chat runs before socket close, in-process SIGUSR1/config restart without orphan child, `session_end` shutdown/restart finalizer (bounded drain), leftover adapter-process kill on restart *(5.x–7.1)* | ❌ | `gateway/server.rs` + `cli/mod.rs` |
| Versioned state migrations + plugin convergence before Gateway readiness; fail closed with `doctor --fix` guidance; migration-lease release on exit *(7.1)* | ❌ | new `infra/state_migrations.rs` |
| Delivery recovery: paced outbound + restart-continuation replays after outages (wall-clock budget, no rate-limit bursts), atomic pre-connect send-evidence clearing, no double-replay of sent replies, honest partial delivery status, dead-lettered queue surfacing *(7.1)* | ❌ | `infra/delivery.rs` |
| Auth hardening: default remote auth rate limiter (`gateway.auth.rateLimit`, loopback exempt), fail-closed non-loopback start without shared secret/trusted proxy, no-auth Tailscale exposure rejection, abort active runs on auth removal (`stopReason:"auth-revoked"`), browser-token expiry after rotation, invalidated device-token RPC rejection, forged loopback origin rejection, malformed HTTP/WS target rejection (GHSA-6hc3-f4rg-377m) *(5.x–6.x)* | ❌ | `gateway/auth.rs`, `gateway/connect_policy.rs` |
| Session store integrity: canonical terminal-outcome normalization, stale/pre-reset lifecycle event rejection, agent-scoped session lookups (multi-agent leak), phantom `sessions.list` rows hidden, malformed persisted rows quarantined → doctor, dead-main-session recreation without stale metadata *(6.x)* | 🚧 | All six behaviors implemented + tested in `sessions/lifecycle.rs` (`normalize_terminal_outcome`, `apply_lifecycle_event` epoch rejection) and `sessions/store.rs` (`list_for_agent`, phantom-row hiding, `quarantine_malformed`→doctor surface, `recreate_main_session`). Remaining: gateway runtime still serves from the in-memory `SessionStore` — wire it over the SQLite store |
| Session write locks: release on fence-read throw/manual abort/timeout/teardown, max-hold reclaim at acquisition, stale-lock reporting, `session.writeLock.acquireTimeoutMs`, compaction watchdog bounded to compaction timeout *(5.x–6.x)* | 🚧 | `sessions/lock.rs`: RAII guard releases on all drop paths (incl. task abort), max-hold reclaim at acquisition (generation-fenced, no permit leak), `StaleLockReport` drain, `acquireTimeoutMs`/`maxHoldMs` config — tokio-time tested. Remaining: compaction-watchdog bound (agents-cluster compaction runtime) |
| Sessions read perf: bounded `sessions.list` + truncation metadata + row memoization, streaming transcript readers (forward/reverse), `sessions.usage` aggregate totals from all sessions + transcript-estimated context budget when provider usage missing, subagent history read caps, background-refresh of large usage summaries *(5.x–7.1)* | 🚧 | `sessions.list`/`sessions.usage` RPCs exist; streaming transcript readers (forward/reverse, bounded) ✅ `sessions/transcript.rs`; list bounding in `gateway/sessions_rpc.rs` (gateway cluster); usage estimation/aggregation + subagent read caps ❌ |
| Systemic timeout hardening: cap extremely large / zero / negative timeouts across commands, image understanding, file locks, queued tasks, auto-replies; SDK `timeoutMs:0` disables client watchdog *(6.11)* | ❌ | shared timeout clamp helper in `infra/` |
| Hot-path perf: immutable plugin-metadata/catalog/auth-store snapshot reuse, lazy-loaded core handlers, `agentRunCache` cap, usage-cache concurrency bounds, gateway lock fd leak fix, noop-config-reload snapshot commit *(6.x–7.1)* | ❌ | `gateway/server.rs` + `plugins/loader.rs` |
| Diagnostics: outcome-driven stuck-session recovery + `diagnostics.stuckSessionAbortMs`, abort-drain wedged runs, startup phase spans, bounded async diagnostic queue w/ drop summaries, malformed base64/Host rejection across file-transfer/media/browser-proxy/node-invoke *(5.x–6.x)* | ❌ | `gateway/server.rs` + `logging/` |
| Trust/security: trusted package sources reject lookalike sibling paths, no auth-token forwarding on cross-origin redirects, internal HTTP session/model overrides require admin, non-owner loopback tool restriction, approval-sensitive tool policies survive registry/hook changes, open-DM tool exposure audit *(6.x)* | ❌ | `gateway/auth.rs` + tool policy |
| TLS/transport: blank cert/key path rejection, transient pre-hello close tolerance, SIEM security events + stdout OTEL log exporter, config watcher polling fallback when inotify exhausted *(6.x)* | ❌ | `gateway/server.rs` |
| Node/device pairing: pairing flood/stale-approval fixes, `operator.admin` required for `browser.proxy` nodes, node `--context-path` reverse proxy, Tailscale-Serve fallback route advertisement, exec approvals delivered past busy chat queue, native Windows exec approvals, refresh paired-device last-seen *(6.x–7.1)* | ❌ | `gateway/connect_policy.rs` + `infra/exec_approval.rs` |

---

## Agents (`src/agents/`, `src/gateway/chat.rs`, `src/sessions/`)

| Item | Status | Notes |
|------|--------|-------|
| Automatic fast mode (`/fast auto`): short calls start fast, longer/fallback work returns to normal; effective (not configured) state visible in status/ACP sessions through fallback; per-model automatic cutoff *(6.10–6.11)* | ❌ | `gateway/chat.rs` + `agents/mod.rs` |
| Generated session titles from first message + default/per-agent `utilityModel` routing for cheap title/topic generation + session-label fallback chain *(7.1)* | 🚧 | Deterministic half done in `sessions/mod.rs`: `derive_title_from_first_message` (collapse/strip/word-boundary truncate) + `resolve_session_label` fallback chain (explicit → derived → key). Remaining: `utilityModel` config + LLM title/topic pass (providers/chat clusters) |
| Tool Search bridge for large tool catalogs + deferred `spawn_agent` retrieval via `tool_search` + control tools kept available on misroute *(5.x–7.1)* | ❌ | new `agents/tools/tool_search.rs` |
| QuickJS-WASI code mode: `exec`/`wait`, internal namespaces + exact dispatch, typed MCP code-mode API, per-agent config, structured timeout/unavailable errors, disabled when exec host is `node` *(5.x–6.x)* | ❌ | new `agents/code_mode.rs` |
| Turn recovery: retry thinking-only + empty post-tool finals, truncated-API-response detection (no silent hang), silent local streams time out on missing first event, mid-stream error recovery pump, mismatched replayed tool request/result resume, client-disconnect abort not misclassified as timeout *(6.x–7.1)* | ❌ | `gateway/chat.rs` stream loop |
| Terminal outcomes: canonical terminal-outcome normalization (replace prose classifiers), provider-failure terminal state preserved, `delivery.mode=none` honored in retry selector, `/stop`/abort clears queued follow-ups + ends subagents, stale abort markers ignored for fresh events *(6.x)* | 🚧 | Sessions half done: `sessions/lifecycle.rs` canonical normalization (`normalize_terminal_outcome`) + epoch-fenced stale-event rejection (stale abort/lifecycle markers ignored for fresh epochs). Remaining: chat-loop integration (`gateway/chat.rs`), delivery-mode retry selector, `/stop` follow-up clearing |
| Compaction ownership: OpenClaw-owned safeguard compaction incl. CLI backends (disable Pi auto-compaction), post-compaction loop guard (`tools.loopDetection.postCompactionGuard.windowSize`), retry-from-compacted-transcript when no final reply, default compaction timeout 180s, compaction planning off the event loop *(5.x–6.x)* | ❌ | new `agents/compaction.rs` |
| Compaction sizing: reserve clamp to model `maxTokens` + small-local-model cap, tool-heavy prompt pressure estimation at LLM boundary, overflow budgets derived from provider errors, nested tool-result turns counted in preflight, CJK-aware + harness-role token estimation, memory flush before preflight compaction, post-compaction token snapshot preservation (stop repeat auto-compaction) *(6.x–7.1)* | ❌ | `agents/compaction.rs` |
| Prompt-cache stability: stable user-turn serialization, deterministic ordering, append-only history mid-session + tail runtime-context message, ChatGPT `session_id` affinity header, per-tool-result size caps (configurable for large-context models), dense delta snapshot trimming *(6.x–7.1)* | ❌ | `gateway/chat.rs` prompt builder |
| Failover engine: state-aware lane suspension + persisted quota resume, idle watchdog during pre-stream setup escalating via profile rotation, cost-runaway breaker (halt after 5 consecutive idle timeouts without progress), auth-profile rotation before cooldown marking, no cooldown on format-level rejections, stale-`errorMessage` false-failover suppression *(5.x)* | ❌ | `agents/model_fallback.rs` |
| Subagents: `sessions_spawn` task in child's first visible message, `agents.defaults.subagents.delegationMode` (`suggest`/`prefer`) + `model`, `announceTimeoutMs`/`archiveAfterMinutes`, registry persisted in SQLite, handoffs via agent steering queue (announced only after parent transcript write), blocked/progress-only completions marked errors not success, killed runs not shown running, steered task text preserved on restart redispatch, list filters (`requesterSessionKey`) *(5.x–7.1)* | ❌ | `agents/tools/subagents.rs` + `sessions/` |
| CLI-backend runtimes (claude-cli / gemini-cli / Pi): native compaction ownership generalized, native Bash permission routed through OpenClaw exec policy (authoritative), empty CLI replies → `empty_response`, soft-resume on prompt drift, exit-0 failover + bounded reseed + image sweep, wedged-turn crash prevention, native thinking with `/reasoning` gating, OAuth-expiry re-auth hint, resume-binding reset on `/reset` *(6.x–7.1)* | ❌ | `agents/acp.rs` + new `agents/cli_backend.rs` |
| Heartbeat overhaul: multi-agent cadence fixes (parallel wake fan-out, agent-scoped `skipWhenBusy`, `HEARTBEAT.md` directives always appended, timer clear on throw), active-hours-aware phase scheduling with timezone, `heartbeat_respond` first-valid stop, `session.dmScope=main` wake routing, persistent heartbeat session bindings, reasoning-capable heartbeats show intended reply not internal reasoning, scheduler-silently-not-firing regression fix, stale Current-time refresh *(5.x–7.1)* | ❌ | new `agents/heartbeat.rs` |
| Visible replies: `messages.visibleReplies:"automatic"` default (group/channel final delivery), `message_tool` opt-in for ambient rooms, structured final preferred over streamed fragments, `NO_REPLY` restricted to auto group/channel, same-source message-tool sends mirrored into transcript *(6.1)* | 🚧 | `config/types.rs` has `visible_replies: Option<bool>` — needs enum (`automatic`/`message_tool`) + chat-loop enforcement |
| Reply sanitization: strip `<final>`/`<function_response>`/runtime-context sentinels, serialized tool-call text, `[TOOL_CALL]` blocks, MiniMax `mm:` reasoning tags, DSML/XML scaffolding from user-facing replies and replay *(5.x–7.1)* | ❌ | new `agents/reply_sanitize.rs` |
| Tool-call parsing hardening: record-shaped streaming args for non-object JSON, `\u`-escape + smart-quote repair, malformed arg-value suffix strip, surrogate-range HTML entity preservation, non-array tool-result normalization at ingest, unsupported dynamic/MCP schema quarantine, permissive array-item schemas before provider submit, ReDoS guards (glob-to-regex, session names) *(6.x–7.1)* | ❌ | `agents/tools/common.rs` |
| Tool policy: per-sender tool policies (channel-scoped sender keys), `tools.message.crossContext` / `tools.message.actions.allow`, `tools.profile:"full"` grants optional plugin tools, `tools.exec.security` authoritative over model args, message-tool spam-loop block (volatile IDs), `tool.call.failed` events for blocked/failed terminal tools, abort no-progress repeated tool loops *(5.x–6.x)* | ❌ | `agents/tools/mod.rs` policy layer |
| Lean-model tool surface: keep `exec` visible for lean local models, trim media/TTS/PDF tools in lean mode, skip tool prep for toolless models, spill truncated `web_fetch` output to file *(7.1)* | ❌ | `agents/tools/mod.rs` |
| Session identity/store: mixed-case session-key migration at startup, custom/per-agent `session.store` reply routing, session metadata → SQLite, labels preserved across rollover, sender metadata + lineage in transcript JSONL, `/name` session rename, per-DM-contact model assignment, behavior overrides across daily/idle rollover *(6.x–7.1)* | 🚧 | Done in `sessions/store.rs`: mixed-case key migration at startup (`migrate_mixed_case_keys`, collision→quarantine) + session metadata → SQLite. Remaining: `session.store` reply routing, labels across rollover, transcript JSONL sender/lineage metadata, `/name` rename, per-DM-contact models, rollover behavior overrides |
| Run state: runtime overrides + fallback tool-call hints + mixed image attachment order preserved across CLI fallback, one resolved capability profile per run + scoped conversation profiles, sender-deduped user messages, phase-aware `agent.wait` attribution, hard-timeout snapshot preservation *(6.x–7.1)* | ❌ | `gateway/chat.rs` |
| Bootstrap/identity: per-agent bootstrap profiles (`contextInjection`, `bootstrapMaxChars`, `bootstrapTotalMaxChars`), provider/model identity injected into system prompts, hook-injected `BOOTSTRAP.md`, `OPENCLAW_WORKSPACE_DIR` resolution, prompt-preface body delimiters *(5.x–6.x)* | ❌ | `agents/mod.rs` |
| Media-gen async task lifecycle: shared task lifecycle (status, dedupe, message-tool completion delivery), completions routed through requester agent, group music/video require visible delivery, async starts don't end Codex turn, results return to requesting chat when task starts without full conversation target *(6.x)* | ❌ | `media/mod.rs` + tasks ledger |
| Managed git worktrees: automatic per-task worktrees (create/snapshot/restore/GC), new chat session in managed worktree *(7.1)* | ❌ | new `sessions/worktree.rs` — not started in the 2026-07 sessions pass (large self-contained git feature; needs its own slice: git CLI worktree lifecycle + snapshot/GC policy + session binding) |
| Cross-agent access: list all required visibility/agent-to-agent/allowlist settings when blocked, canonical node-id resolution before bound-node comparison, `session.agentToAgent.maxPingPongTurns` up to 20 *(5.x–6.x)* | ❌ | `agents/tools/sessions_a2a.rs` |

---

## Cron (`src/cron/`)

| Item | Status | Notes |
|------|--------|-------|
| Cron store → SQLite: doctor-managed migrations (legacy run-log / array-`jobs.json` / archived-store compat), path reported in `cron.status`, legacy archive recovery across devices *(6.x–7.1)* | ❌ | `cron/mod.rs` + state DB |
| New scheduling models: on-exit schedules (wake on watched command exit), event triggers via polled condition-watcher scripts (code mode), detached session-targeted runs, declarative jobs with owner attribution + richer status *(7.1)* | ❌ | `cron/mod.rs` scheduler |
| Per-job model control: fallback models / disable-fallbacks / inherit via CLI, `cron edit --clear-model`, `payload.model` + `timeoutSeconds` honored, per-job agent-turn model selection, subagent model fallbacks, provider/model preserved on timeout rows *(6.x–7.1)* | ❌ | `cron/mod.rs` + `cli/mod.rs` |
| RPC/CLI surface: `cron.get`, `cron get <id>`, `cron list --agent`, `status` in `--json`, `cron run --wait`, `cron.runs --run-id`, `--wake now`, sub-second/`+5m`/timezone `--at` *(5.x–6.x)* | ❌ | `gateway/websocket.rs` + `cli/mod.rs` |
| Scheduling integrity: add/remove doesn't drop another due run, startup catch-up deferral ids persisted (no read-RPC clobber), stale future `nextRunAtMs` repair, tz/`staggerMs` preserved when `--cron` replaces expression, invalid absolute timestamps + sub-ms durations rejected, live scheduler rollback on persist failure *(6.x–7.1)* | ❌ | `cron/mod.rs` |
| Retry classification: `retryOn: ["server_error"|"network"]` (EAI_AGAIN/EHOSTUNREACH), transient rate-limit retries + preflight model fallbacks, isolated-setup-timeout retry + gateway restart, execution-milestone timeouts (dispatch/context/spawn/tool-start) with phase-specific errors, lane-timeout prevention during long tools, stuck cloud-model call recovery *(5.x–6.x)* | ❌ | `cron/mod.rs` |
| Delivery/isolation: `deleteAfterRun` removes temp session+transcript, default `runMode:"due"`, keyless implicit isolated delivery refused, delivery routing fields cleared on edit + implicit mode preserved on partial edits, blank targets rejected, `delivery.mode:"none"` previews, thread-id type preserved through SQLite, announce control-reply suppression, scheduled-message context carries forward without exposing cron internals *(5.x–7.1)* | ❌ | `cron/mod.rs` dispatch |
| Concurrency: cron-owned wake lane (doesn't block human main session), `cron.maxConcurrentRuns` default 8, concurrency restore after quota/circuit-breaker resume *(6.1)* | ❌ | `cron/mod.rs` |
| Isolated-run diagnostics: per-run session attribution events, isolated model usage diagnostics, tool-policy failure diagnostics, in-flight/failing job doctor warnings *(6.x)* | ❌ | `cron/mod.rs` + doctor |

---

## Memory (`src/memory/`)

| Item | Status | Notes |
|------|--------|-------|
| QMD backend: `memory_search` in transient QMD mode, rerank toggle, timed-out search background-work stop, serialized per-store update/embed writes (SQLITE_BUSY), Windows absolute command paths + child-process kill, zero-hit search-sync skip *(6.x–7.1)* | ❌ | new `memory/qmd.rs` |
| Embeddings: OpenAI 431 batch splits, per-dimension separate indexes (Ollama), `outputDimensionality` truncation for local GGUF, GGUF worker-sidecar isolation (degrade to keyword), provider-unavailable fail-fast + no FTS downgrade on transient outage, timed-out call aborts, owning plugin loaded at startup *(6.x)* | 🚧 | `memory/embeddings.rs`, `memory/batch_embedding.rs` exist — new hardening ❌ |
| sqlite-vec: platform-variant fallback + `memorySearch.store.vector.extensionPath`, kNN k clamp to 4096, load-failure vs missing-embeddings distinction, bounded rowid-batch fallback scan *(5.x–7.1)* | ❌ | `memory/hybrid.rs` |
| Index/state hygiene: stale reindex temp/rollback-journal sidecar cleanup (incl. NFS), avoid SQLite WAL on network filesystems + passive periodic checkpoints, full-reindex rollback recovery, index identity validation, Windows EBUSY retry *(5.x–6.x)* | ❌ | `memory/manager.rs` |
| Memory Wiki security + robustness: session visibility enforced on `wiki_search`/`wiki_get` (GHSA-72fw-cqh5-f324), admin/write scope for ingest/Obsidian, code-fence-aware wikilink extraction, unparsable YAML frontmatter tolerated, reserved index-page stem disambiguation, transient page-read retries *(5.x–7.1)* | ❌ | new `memory/wiki.rs` |
| Daily-file handling: matcher widened to `memory/YYYY-MM-DD-<slug>.md` across Dreaming/rem-backfill/harness/doctor/promotion, dreaming promotion caps on `MEMORY.md`, per-day `daily-ingestion.json` *(5.x)* | ❌ | new `memory/dreaming.rs` |
| Dreaming: REM focus on live light-staged memories + shadow-trial scoring, orphaned dreaming-session reclaim, CJK-aware dedupe, nightly recall skip (45s timeout avoidance), no artifact writes when disabled, phase status display (`memory status`), fallback dream diary *(6.x)* | ❌ | `memory/dreaming.rs` |
| Recall/continuity: transcript coverage aligned with session stores + agent ownership, image-heavy long-session continuity, summaries exclude raw tool blocks/media placeholders/`NO_REPLY`, skipped short-term hit explanations, warm transcript read cache, long-workspace-path resume recovery *(6.x)* | ❌ | `memory/search.rs` + `sessions/` |
| Active Memory: `toolsAllow` recall tool names, `status=no_relevant_memory` for NONE, scoped `:`-containing conversation-id skip, `setupGraceTimeoutMs` end-to-end, graceful skip without backend, per-conversation filters *(5.x + 5.2-carryover)* | ❌ | new `memory/active.rs` |
| LanceDB: CJK + literal auto-capture triggers, `memory_recall` for `plugins.slots.memory: memory-lancedb`, corpus label alignment, rotated/deleted archive indexing, arrow peer alignment *(5.x–7.1)* | ❌ | `memory/lancedb.rs` |
| `memory_search` provider plumbing: generic embedding providers honored, `memorySearch.provider=none` skips capability discovery, unindexed transcript detection in status mode, `embeddingProviders` capability contract *(6.x–7.1)* | ❌ | `memory/search.rs` |
| Watcher/scale: chokidar-polling removal (native watchers, EMFILE fan-out reduction), shared skill dirs watched once, transient read retries, generated transcript paths rewritten on rollover *(6.1)* | ❌ | `memory/manager.rs` |

---

## TTS / Speech (`src/tts/`)

| Item | Status | Notes |
|------|--------|-------|
| Unified Talk session controller + `talk.session.*` RPCs shared across realtime relay, transcription relay, Voice Call, Google Meet; `talk.realtime.instructions`; provider/transport from `talk.catalog`; bounded lifecycle metrics without transcript/audio leakage *(5.x)* | ❌ | `tts/mod.rs` + new `talk/` module |
| Gateway `tts.speak` RPC (inline whole-clip audio) + OpenAI MP3 TTS routed through voice delivery + `/tts audio` rendered as playable attachments *(6.x–7.1)* | ❌ | `tts/mod.rs` + `gateway/websocket.rs` |
| Realtime voice: shared realtime SDK (turn-context tracking, output-activity/barge-in cancellation, consult-question + activation-name matching), `gpt-realtime-2`/`-2.1` defaults + GA WS shape, WebRTC assistant transcripts, 16 MiB payload bound, malformed tool-name filtering at OpenAI/Azure/Google boundaries, double-greeting fix, Codex OAuth reuse *(5.x–7.1)* | ❌ | new `tts/realtime.rs` |
| TTS directives: `[[tts:speed]]`, directive transform before message-tool sends reach delivery, supplements across live-preview channels without duplicated text, preferred provider aliases for `[[tts:*]]`, inbound audio preserved for message-tool TTS, explicit `[[tts:text]]` blocks *(5.x–6.x + 5.2-carryover)* | ❌ | new `tts/directives.rs` |
| Voice Call: Twilio IE1/AU1 regions, per-call/per-number agent routing + `sessionScope:"per-call"`, webhook transcript auto-respond, persisted call store resolution, early TTS via `onEarlyText` before compaction wait, DTMF/webhook parsing, Telnyx realtime media streaming, stale stream reaper, shared webhook replay tracking *(5.x–7.1)* | ❌ | new `channels/voice_call.rs` |
| Provider bits: MiniMax explicit decodable audio format + `base_resp` error envelope + volume bounds, ElevenLabs current model IDs + streamed TTS, Xiaomi MiMo voicedesign, provider-backed voice model catalogs, realtime/talk SecretRef resolution, API-key auth required for realtime *(5.x–7.1)* | ❌ | `tts/mod.rs` providers |
| STT: sherpa-onnx empty structured-transcript skip, non-English STT hint prompts (no English audio prompt) *(6.x–7.1)* | ❌ | `media/mod.rs` transcription |

---

## Plugins (`src/plugins/`)

| Item | Status | Notes |
|------|--------|-------|
| Channel/provider externalization wave: Bedrock/Mantle, Slack, WhatsApp, Matrix, Discord, Copilot, Brave, ACPX, Google Chat, LINE, OpenShell sandbox, Tokenjuice, PixVerse moved to on-demand npm/ClawHub plugins with npm-first catalogs *(5.x–6.x)* | ❌ | architectural: mylobster bundles these natively — decide facade vs keep-bundled per module (`plugins/manifest.rs`) |
| Managed install lifecycle: npm-root install/rollback/repair/uninstall with legacy-peer resolution + `openclaw` peer relinking, install-record ledger recovery, registry metadata hashing, `npm-pack:<path.tgz>` installs, ClawHub 429 reset-window hints, bounded archive downloads *(5.x–7.1)* | ❌ | `plugins/discovery.rs` + `plugins/loader.rs` |
| Operator install policy: replaces dangerous-code scanner (doctor checks, CLI wiring, ClawHub metadata), fail-closed on trusted-policy errors, untrusted workspace plugins blocked across restarts + fail-closed in setup discovery, install-time code-safety scans scoped to entrypoints *(6.x)* | ❌ | `plugins/manifest.rs` policy layer |
| Plugin state → SQLite: install index + per-plugin state slices/KV, plugin state consolidation onto SDK seams, QQBot credential-backup migration *(5.x–7.1)* | ❌ | `plugins/mod.rs` + state DB |
| Plugin SDK session surface: grouped session actions, `sendSessionAttachment`, cron-backed `scheduleSessionTurn`, `api.runtime.llm.complete`, `extractStructuredWithModel`, `SessionEntry` slot projection, row-level session helpers (deprecate `loadSessionStore`), runtime active-model metadata to tool factories *(5.x)* | ❌ | `plugins/api.rs` |
| Hook surface: `before_agent_run` block gate, `before_agent_finalize` retry, `resolve_exec_env`, typed `session_end` (`shutdown`/`restart`), `plugins.entries.<id>.hooks.timeoutMs`, `before/after_compaction` 30s timeout, `contextTokenBudget` on `llm_output`/`model_call_*`, native sender/conversation IDs through hooks, reply-payload send hook, channel pairing-request hook, `gateway_stop` alias *(5.x–7.1)* | 🚧 | `hooks/mod.rs` (has `before_agent_finalize` + `cron_changed` from v4.29 work; rest ❌) |
| Registry/runtime: live hook-registry view for tool-call hooks, pinned session-extension registry (no `sessions.pluginPatch` failure after load churn), convergence after RPC git updates, `plugins.allow` guidance with ready-to-copy example, external channel plugin discovery at startup, gateway hot-reload on enable/disable *(6.x + 5.2-carryover)* | ❌ | `plugins/loader.rs` |
| Install reliability (7.1): ClawHub-switched plugins preserved during core updates, bundle payloads validated by manifest contract, git-clone staging on target fs (EXDEV), unloadable channel plugins + disabled reasons in `/status plugins`, source-only bundled entry resolution *(7.1)* | ❌ | `plugins/discovery.rs` |
| MCP: pending approvals persisted with TTL sweeper, `tools/call` AbortSignal forwarding, structured/non-text content in tool results, draft-2020-12 schema diagnostics, non-object argument rejection, stdio stderr fail-fast attributable death, env filtering + redirect header scrub, owner required for permission replies *(5.x–7.1)* | ❌ | new `infra/mcp.rs` |
| New plugins: Logbook work-journal (disabled by default), clickclack durable activity rows, Workboard typed hook contract, opt-in default-off `admin-http-rpc` *(6.x–7.1)* | ❌ | new plugin modules |
| Authoring: `defineToolPlugin` + `plugins build/validate/init`, marketplace manifest icon field, manifest model-pattern regex validation (safe-regex), managed version drift reporting *(5.x–6.x)* | ❌ | `plugins/manifest.rs` + `cli/mod.rs` |

---

## Skills (`src/skills/`)

| Item | Status | Notes |
|------|--------|-------|
| Skill Workshop pipeline: pending proposals w/ versioned frontmatter, apply/reject/quarantine via guarded review, approved support files (scanner/hash/rollback), `skill_workshop` agent tool, decidable/non-wedging lifecycle approvals, creator cannot bypass proposals, stale-symlink retarget, snapshot refresh after restart *(5.x–7.1)* | ❌ | new `skills/workshop.rs` |
| `/learn` skill drafting from recent work + auto-capture routes corrections to existing skills + reusable-workflow save suggestions + background lifecycle curator *(7.1)* | ❌ | `skills/workshop.rs` |
| ClawHub skill installs: `@owner/<slug>` refs (drop bare-slug), GitHub-repo installs via resolved API (pinned commit + policy checks), install safety policy honored, `skills update --all` tracked-only, verified source provenance on readback, bounded marketplace responses *(6.x)* | ❌ | `skills/mod.rs` install path |
| Skill loading security: opt-in archive upload (`skills.install.allowUploadedArchives`), `skills.load.allowSymlinkTargets`, plugin-skills symlink roots with fail-closed containment, watcher traversal depth caps, sandbox skill prompt rebuild from sandbox copy *(5.x)* | ❌ | `skills/mod.rs` |
| Core skills index: centralized loading/status/filtering/prompt formatting, hydrated `resolvedSkills` cache keyed by redacted config (runtime-only, not persisted), watched-root refresh, `/skill` loads workspace skills, `openclaw.os:macos` → Darwin *(5.x–6.x)* | ❌ | `skills/manifest.rs` |
| Skills CLI/doctor: `skills check --agent` per-agent visibility, platform-incompatible vs missing-requirements split, `--global` installs, empty-name rejection, deterministic `.skill` package order *(5.x + 5.2-carryover)* | ❌ | `cli/mod.rs` + doctor |
| Bundled skill fixes/additions: meme-maker, node/python debugging, diagram, autoreview; `gog` via Homebrew, `1password` no forced tmux, `trello` requires `curl` *(5.x–6.x)* | ❌ | skills assets |

---

## Routing / Auth (`src/routing/`, `src/infra/secrets/`, exec approval)

| Item | Status | Notes |
|------|--------|-------|
| Structured SecretRef provider auth: resolution only through `secrets.providers[id]`/`secrets.defaults` (no broad env-string inference), `keyRef`/`tokenRef` preserved on `secrets apply`, dead-owner auth-profile lock reclaim, Bitwarden `BWS_SERVER_URL` *(5.x–6.x)* | 🚧 | `infra/secrets/resolver.rs` exists — structured provider-marker semantics + lock reclaim ❌ |
| Credential storage: secrets in `~/.openclaw/credentials/`, auth profiles in per-agent SQLite store, symlinked credential file rejection (Telegram/LINE/Zalo/IRC/Nextcloud), atomic crash-safe `auth.json` writes, serialized provider login writes *(5.x–6.x)* | ❌ | `infra/secrets/` + state DB |
| OAuth lifecycle: refresh failures report reauth (not stale success), contention diagnostics, model availability refresh after renewal, provider auth refresh after CLI login, copied auth-profile order preserved, skip refresh without refresh token *(6.x–7.1)* | ❌ | new `providers/oauth.rs` |
| Auth-profile failover classification: `deactivated_workspace` / HTML auth (`auth_html`) / Moonshot 429-billing permanent, plain-language exhausted-failover recovery, legacy auto-fallback pin clearing, periodic primary reprobe for auto-pinned fallback sessions with announce/clear *(6.1)* | ❌ | `agents/model_fallback.rs` |
| Exec approvals: realpath-bound allowlists/safe-bin/Allow-Always (symlink retarget defense), `/approve` through trusted runtime, async approval follow-up nonces, denied exec terminal (+CJK stop phrases), safe-shell-builtins allowlist mode, `argPattern` + `..` canonicalization on all OSes, package-manager approvals bound to inner commands, jq/inline-eval safe-bin semantics *(5.x–7.1)* | ❌ | `infra/exec_approval.rs` |
| Bundled Policy plugin: channel-conformance/sandbox-posture/ingress/data-handling checks, doctor lint findings, opt-in workspace repair *(5.x–6.2)* | ❌ | new `routing/policy.rs` |
| Access groups/allowlists: `accessGroup:*` before numeric checks, parent-group false-positive fix, malformed paired list normalization, direct-only targets kept out of group sessions, bare runtime channel-id matching *(5.x–6.x + 5.2-carryover)* | ❌ | new `routing/access_groups.rs` |
| Channel-scoped auth: channel model overrides honored in ingress, read-target allowlists enforced, scoped mention-pattern policy, ClickClack `allowFrom` before dispatch, session auth-profile overrides preserved across same-provider model switch, `dmScope` config reload in slash routing *(6.x)* | ❌ | `routing/mod.rs` |
| Prompt-injection boundaries: untrusted group metadata as structured untrusted context, fetched file text wrapped as external content, queued system-event sanitization (no nested prompt-marker spoofing), owner downgrades in metadata not prompt text, transcript metadata field-name escaping *(5.x–6.x)* | ❌ | `gateway/chat.rs` prompt assembly |
| Model-selection routing: `/new`/`/reset` uses current default (not cached), auto-fallback model surfaced in `status`/`session_status`, model-not-found hints detect bundled/legacy providers, expired-token log flooding suppression, cron auth-failure policy isolated from shared cooldowns *(6.x–7.1)* | ❌ | `providers/mod.rs` + `gateway/chat.rs` |

---

## Web / Browser / Fetch (`src/browser/`, `src/agents/tools/web_*`)

| Item | Status | Notes |
|------|--------|-------|
| Browser SSRF: current-URL checks before act/read/snapshot/screenshot/export (only OpenClaw navigations close on denial), snapshot tab-URL validation before CDP, configured CDP control-host hostname allowlist without widening nav policy, browser-internal CDP target filtering, policy-denied tab quarantine *(5.x–7.1)* | ❌ | `browser/mod.rs` |
| Managed Chrome lifecycle: cookie persistence across restarts (non-interactive encryption key, CDP close before signal), stale CDP listener recovery, `openTab` return (wsUrl race), Chrome MCP crash cancellation, foreground-before-screenshot + private-by-default screenshots, peer-owned tab close on session reset, profile config hot-reload, `cdpPort` kept when `cdpUrl` omits port *(6.x–7.1)* | ❌ | `browser/mod.rs` |
| Chrome extension relay driver: loopback relay (no remote-debugging prompt), direct pairing to remote gateway *(7.1)* | ❌ | new `browser/extension_relay.rs` |
| Browser tool surface: `--labels` overlay for full-page/element captures, modal-dialog snapshots + `browser dialog --dialog-id`, `browser evaluate --timeout-ms`, validated download actions (managed URL/filename/path), upload from inbound media dir, screen snapshots as media, reference-rich snapshots, `OPENCLAW_EAGER_BROWSER_CONTROL_SERVER` *(6.x–7.1)* | ❌ | `agents/tools/browser_tool.rs` |
| `web_search`: bundled Parallel provider (`api.parallel.ai/v1/search`, zero-config MCP), key-free providers (Parallel Free/DuckDuckGo/Ollama/Codex Hosted) as explicit opt-ins, Brave `tools.web.search.apiKey` → plugin-entry migration, startup activation of configured provider, `query` marked required, native tool policy enforcement *(5.x–6.x)* | 🚧 | `web_search/parallel.rs` (REST transport, query/objective caps, NUL-delimited cache keys, generated-session-id cache strip) + `web_search/duckduckgo.rs` (key-free HTML provider, uddg decode, bot-challenge detection) ✅; both explicit opt-ins (default stays Brave) ✅; Brave key scoped to `brave.apiKey` over legacy top-level ✅; `query` required in schema ✅; Parallel-free MCP transport, startup provider activation + native tool policy enforcement ❌ (gateway/plugin-runtime concerns) |
| `web_fetch`: late-bound config, `tools.web.fetch.useTrustedEnvProxy`, truncated output spilled to file, bounded body reads (`fetchHttpJson` + body-less snippets), SSRF loopback block for trusted hostname origins, guarded fetch before managed-proxy DNS, global/largest-model timeout for link fetches *(5.x–7.1)* | 🚧 | `agents/tools/web_fetch.rs`: `useTrustedEnvProxy` (env proxy + NO_PROXY-aware, proxy owns DNS) ✅; guarded static SSRF check always before proxy/DNS ✅; bounded streaming body reads (`read_body_bounded`) ✅; truncated output spilled to file (`fullContentPath`) ✅; late-bound config ✅ by construction (config read per call from ToolContext); trusted-hostname-origin loopback block + link-fetch model timeouts ❌ (no hostname-allowlist/link-fetch subsystem yet) |
| Proxyline managed proxy stack: replaces proxy-agent interception, `proxy.loopbackMode`, `Retry-After` honored, IPv4/IPv6 SSRF pinning, RFC2544/ULA fake-IP DNS handling, CIDR/wildcard `NO_PROXY`, local CDP/DevTools proxy bypass (both `NO_PROXY` casings), FormData conversion before proxied fetches *(5.x–6.x)* | ✅ | `infra/proxy.rs`: `proxy.loopbackMode` (`gateway-only`/`proxy`/`block` via `resolve_proxy_route`), `parse_retry_after` (delta-seconds + HTTP-date, past→0), full undici-parity `matches_no_proxy` (comma+whitespace split, wildcard/leading-dot incl. apex, `:port`, bracketed/bare IPv6, IPv4 CIDR + octet wildcards), lowercase-shadows-uppercase env reader (CDP/DevTools bypass both casings), multipart FormData normalization before proxied fetches; fake-IP RFC2544/ULA DNS handling via `web_fetch.rs::SsrfPolicy`; config: `Config.proxy` (`ProxyConfig`) |
| Documents/media pipeline: ClawPDF extraction incl. encrypted PDFs + 120s idle timeout on remote reads, `.docx`/`.pptx`/`.xlsx` correct extensions, HEIC/HEIF→JPEG before description, byte-sniff `input_file`/`data:image/*` (reject spoofed MIME), Rastermill image ops (sips/ImageMagick/GraphicsMagick/ffmpeg fallback), URL-basename decoding, large managed PDFs through media-understanding *(5.x–6.x)* | ❌ | `media/mod.rs` + `agents/tools/pdf_tool.rs` |
| Google Meet: force English UI via `hl=en`, bounded JSON reads, `accessType`/`entryPointAccess` *(7.1 + 5.2-carryover)* | ❌ | new `channels/google_meet.rs` |

---

## CLI / Doctor (`src/cli/`, `src/infra/doctor.rs`)

| Item | Status | Notes |
|------|--------|-------|
| New CLI commands: `openclaw attach` (external harness → gateway session), `promos`, `channels list --all` (installed/configured/enabled + `--channel`), `models auth list`, `agent --message-file`, `message --limit` + pagination hints, `sessions export-trajectory`, `config unset --dry-run --json`, `path set --dry-run --diff`, `workboard list`, node `--context-path` *(5.x–7.1)* | ❌ | `cli/mod.rs` |
| Doctor `--fix` migration wave (canonical-config-only runtime): `openai-codex`→`openai`, `oauthRef` sidecars→inline, `lossless-claw`→`plugins.slots.contextEngine`, legacy `tools`→`contracts.tools`, `groupAllowFrom`, cron→SQLite, TTS legacy paths, agent-registry + group-tool-policy schemas, sandbox registry files, `api_key`→canonical, null workspace repair, safe migrations commit even when unrelated validation fails, repeatable migrations *(5.x–6.x)* | ❌ | `infra/doctor.rs` |
| Doctor health findings: disk space, plaintext secrets, tool-schema errors, malformed catalogs, systemd exhaustion/linger, Windows LAN firewall, SQLite WAL runtime verification, channel-plugin blockers, heartbeat template, multi-account default routing, in-flight/failing cron, cron→inactive-channel, volatile SQLite state, UI freshness, `--post-upgrade --json` *(6.x–7.1)* | ❌ | `infra/doctor.rs` |
| Onboarding: classic flow for bare `openclaw` pre-config, localized wizard (EN/zh-Hans/zh-Hant), provider picker order, single OpenAI entry (ChatGPT/Codex/API key), credential masking, `--openai-api-key` passthrough, GPT-5.6 new-setup default, first-run external-provider credential prompt, deterministic providerless fallback *(5.x–7.1)* | ❌ | `cli/mod.rs` onboarding |
| CLI validation/hardening: malformed numeric/timeout/port(>65535)/version/limit rejection, JSON-clean stdout (logs → stderr), `--no-color`/`--log-level` after subcommands, protocol stdout protection at startup, parent options after lazy subcommands, close-command suggestions, shell completion (aliases, core-only during repair), help-path perf, uninstall refuses cwd, UTF-8-valid log tails *(6.x–7.1)* | ❌ | `cli/mod.rs` |
| Status/usage surfaces: `/status` compact model line + fast-mode/systemd state + active-subagent details + context budget, `0` not `?` for fresh context, "1M" formatting, usage footers across rollover, `/usage full` templating + per-turn `usageState`, provider quota in overview, all-zero pricing as missing not `$0`, AWS-SDK cost estimates, OTel provider/model accuracy for slash-qualified ids, usage/quota checks behind HTTP(S) proxy *(5.x–7.1)* | ❌ | `cli/mod.rs` + `gateway/chat.rs` |
| Config CLI/runtime: restart-required vs hot-reloadable metadata (honest `config-patch` notices), prototype-pollution-safe traversal, size-drop guard vs canonical bytes, defaulted provider `baseUrl` preserved on patch, dedup validation warnings across transient reads, hot-reload watcher status, `set`/`patch` persist values equal to defaults + canonical config return, inherited/default unset explanation *(6.x–7.1)* | ❌ | `config/io.rs` + `gateway/websocket.rs` config RPCs |
| Gateway CLI: `probe --timeout`, health/probe `--port`, `--force` detects IPv4-only occupants, `status --json` running version, `logs --follow` auto-reconnect + journal-fallback retry, `usage-cost` multi-agent, stale managed-service repair, device-approve deadlock guidance *(5.x–7.1)* | ❌ | `cli/mod.rs` |

---

## Misc / Infra (`src/infra/`, `src/logging/`, state)

| Item | Status | Notes |
|------|--------|-------|
| SQLite-first state consolidation: shared state DB for auth profiles, session metadata, cron, subagent registry, plugin state/KV, device pairing, dreams, wiki sync, voice-wake, thread bindings, config recovery markers; WAL library verification before opening; orphan temp-file sweep; handle close on exit *(5.x–7.1)* | 🚧 | **Sessions slice implemented** (scoped): `sessions/store.rs::SqliteSessionStore` (rusqlite, WAL, session metadata + lifecycle + skills snapshot, quarantine table, agent-scoped lookups, mixed-case key migration). Other state domains (auth profiles, cron, subagent registry, plugin KV, pairing, dreams, wiki, voice-wake, thread bindings) still file/Postgres-backed — extend the same DB when those clusters consolidate |
| UTF-16/emoji-safe truncation everywhere (`truncateUtf16Safe`): channels, tool results, exec/approval previews, media/audit, web-fetch spill, tasks, status, close reasons *(7.1)* | ❌ | new `infra/text.rs` helper + call sites |
| Subprocess/stream hardening: suppress unhandled stdout/stderr/stdin errors across exec, MCP stdio, SSH tunnels, supervisors, Docker sandbox, ripgrep/find, ffmpeg, secrets exec resolver; EPIPE swallow in command runner *(7.1 + 5.2-carryover)* | ❌ | `agents/tools/bash.rs` + `infra/secrets/exec_provider.rs` |
| Docker sandbox: fail/terminate on broken stdout/stderr capture, custom image tags, credential-named env + container recreation on env-policy change, stdin error handling, GPU passthrough *(5.x–7.1 + v4.29 carryover)* | ❌ | new `infra/sandbox.rs` (mylobster has no docker sandbox yet) |
| Log redaction deltas: HTTP secret fields, auth/cookie headers, OSC escapes, token redaction across chunk boundaries + timeout URLs, credential-bearing URLs in `logs.tail`/connect failures, bare Fireworks keys, libsignal dumps *(5.x–7.1)* | 🚧 | `logging/redact.rs` (v5.2 pattern set ✅; these new patterns ❌) |
| OTel/observability: LLM content spans, one deduplicated `provider.request` timeline event per model call, alertable signals (blocked tools, failover, stale sessions, liveness, oversized payloads), session-key scrubbing from labels, OTLP via env proxy, agent-duration histograms, startup phase spans, critical-memory-pressure stability snapshots *(5.x–7.1)* | ❌ | `logging/mod.rs` + provider call sites |
| Config/state parsing hardening: strict OAuth/token lifetimes, retry-after dates, body sizes, TCP ports, cron epochs, IPv6 `no_proxy`, empty path segments; malformed persisted auth/device/pairing/commitment/task entries skipped → doctor; JSONL malformed-line warnings *(5.x–7.1)* | ❌ | `config/validation.rs` + stores |
| Sandbox/exec env: inherited XDG base-dirs preserved (agent overrides rejected), `tools.exec.pathPrepend` ahead of shell PATH, private OpenClaw temp root for temp workspaces, conda env marker strip, heredoc bodies out of exec summaries, raw command/env omitted from denied-exec logs, `COMSPEC` block *(5.x–7.1 + 5.2-carryover)* | ❌ | `agents/tools/bash.rs` + `infra/` |
| Windows platform hardening: allowlisted-exec paths + `PATHEXT`, case-insensitive inbound paths, netstat/lsof/findstr/taskkill handling, drive-absolute path guards, `SystemRoot`/`ComSpec` resolution, Scheduled-Task recovery *(5.x–7.1)* | ❌ | `infra/security_path.rs` + exec |
| macOS gateway-side: PortGuardian SSH tunnel-record locking (orphaned/dead tunnels), LaunchAgent on external home volumes, SSH `ProxyCommand`/PATH for helpers, SecretRef env-file preservation, WAL checkpoint durability *(7.1)* | ❌ | `infra/` |
| Backup/file transfer: hardlink dereference on creation + unsafe-target rejection on verify, retry temp-archive isolation, volatile cache path skip (Windows EBUSY), late tar-stdin pipe errors, transient "File changed during read" retry *(5.x–7.1)* | 🚧 | `infra/hardlink_guards.rs` exists; retry/volatile-path deltas ❌ |
| Workboard orchestration: agent-coordination primitives, task-backed board runs, worker-dispatch CLI, relational SQLite storage, task comments/details, typed hook contract *(5.31–7.1)* | ❌ | new `workboard/` module |
| Transcripts subsystem: core transcript capture + source providers (auto-start config, manual imports, read-only access, Discord voice source), transcript-backed meeting summaries, provenance metadata, export-trajectory cycle safety *(5.22–5.26)* | ❌ | new `transcripts/` module |
| Heartbeat/goals misc: commitment fan-out prompt scoping, `notify=false` fallback silencing, active session goals in per-turn context, transient fs read-race survival, delivery error catch in heartbeat-ok path *(7.1)* | ❌ | `agents/heartbeat.rs` (with Agents heartbeat row) |
| Text/markdown: CJK-friendly emphasis flanking (`**标签：**正文`), newline-mode paragraph packing *(6.x–7.1)* | ❌ | shared markdown render helper |

---

## Not applicable to the Rust port (tracked for completeness)

Consistent with prior trackers, these surfaces have no mylobster analog today and are excluded from the actionable matrix:

- **Control UI / WebChat** *(all versions)*: session-first sidebar + session groups UI, context ring/cost popovers, reasoning-effort slider + composer redesign, split-pane chat, workspace file viewer, Skills/Cron/Sessions page redesigns, background-tasks + worktrees pages, PWA/service-worker, i18n, CJK IME fixes, streaming render perf, GitHub hover previews, diffs-viewer SSR. Revisit only if mylobster ships a web UI.
- **iOS / watchOS / macOS apps** *(all versions)*: offline caches + outbox, Apple Watch voice turns, Liquid Glass redesign, LaTeX/Markdown rendering, multi-gateway pairing + TLS pinning, push relay, Peekaboo bridge, native macOS session-browser, LaunchAgent UI, App Store/TestFlight work.
- **Android app** *(all versions)*: companion shell, in-app approvals, realtime Talk relay, offline recovery, PTT/Bluetooth mic, signed-APK releases, localization.
- **Conversational onboarding (Crestodian) app-facing UX** *(7.1)*: the agent-loop setup UI across CLI/web-install/macOS app is UI-shell; the gateway-side hooks it relies on are captured in the CLI/Doctor onboarding row.

**Excluded by scope rule (counts only):** ~130 packaging/Docker/dist/installer/npm-shim items, ~120 CI/QA-lab/release-lane items, ~80 dependency-bump/lockfile/pnpm items, ~110 docs-only items, ~80 test-only and dead-code/localization refactors.

---

## Parent backlogs

- `PARITY_v2026.4.29.md` — the v4.1→v4.29 backlog (~100 unchecked items) remains valid.
- `PARITY_v2026.5.2.md` — the v4.29→v5.2 backlog (~185 unchecked items) remains valid.

Both are **parents** of this file: items above are deltas on top of them, and a handful of rows here note *(carryover)* where the v2026.5.3–7.1 window extended a still-missing v4.29/v5.2 feature (LM Studio, QQBot, access groups, Active Memory, TTS directives, Google Meet, docker sandbox, `COMSPEC`). When in doubt, treat those files as the authoritative earlier backlog.
